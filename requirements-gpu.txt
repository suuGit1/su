# GPU requirements for Python 3.9 + NVIDIA CUDA 11.3 (cu113)
# Install with:
#   python -m pip install -r requirements-gpu.txt
# Then verify:
#   python -c "import torch; print(torch.cuda.is_available()); print(torch.version.cuda)"

--extra-index-url https://download.pytorch.org/whl/cu113

# Core scientific stack; numpy<2 keeps compatibility with older PyTorch/cu113 wheels.
numpy>=1.23,<2.0
pandas>=1.5,<2.2
matplotlib>=3.6,<3.9
scipy>=1.9,<1.12
scikit-learn>=1.1,<1.4
tqdm>=4.66
pytest>=7.4

# VPP project dependencies
pydantic>=2.5
pydantic-settings>=2.1
pyyaml>=6.0
structlog>=23.1
pulp>=2.7
cvxpy>=1.3,<1.5
pandapower>=2.13,<2.15

# PyTorch CUDA 11.3 build compatible with Python 3.9
torch==1.12.1+cu113
torchvision==0.13.1+cu113
torchaudio==0.12.1
