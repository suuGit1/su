# 第二阶段实测记录

环境：Linux、Python 3.12、PyTorch 2.5.1+cpu、NumPy 1.26.4、Gymnasium 0.29.1。CPU 单线程，未验证 CUDA 或 Windows。

| 检查 | 结果 |
|---|---|
| 第一阶段环境回归 | 11 项通过 |
| 第二阶段集成和算法契约 | 7 项通过 |
| MAPPO 短训练 | 3 回合 × 8 步，完成更新和保存 |
| 独立进程加载评估 | 3 个新合成场景完成 |
| 局部 critic IPPO 短训练 | 3 回合 × 8 步完成 |
| MAPPO 日时域检查 | 2 回合 × 96 步完成 |

测试覆盖局部观测隔离、critic 维度差异、原始动作概率一致性、终端 bootstrap、实际参数更新、检查点重复评估一致性、CSV 训练执行、重复数据拒绝与配置校验。

参数实际变化且损失有限说明梯度更新生效，不证明收敛。短评估无违规不构成安全保证。合成场景费用不能作为论文性能结论；正式实验需要真实数据、统一对照、充分训练预算、多种子与独立测试。
